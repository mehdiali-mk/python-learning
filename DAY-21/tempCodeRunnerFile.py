    # for snakePieceNumber in range(len(snakeBody) - 1, 0, -1):
    #     newX = snakeBody[snakePieceNumber - 1].xcor()
    #     newY = snakeBody[snakePieceNumber - 1].ycor()

    #     snakeBody[snakePieceNumber].goto(newX, newY)

    # snakeBody[0].forward(20)
    # snakeBody[0].left(45)